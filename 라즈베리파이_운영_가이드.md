# 라즈베리 파이 운영 가이드

## 📋 시스템 개요

이 시스템은 **라즈베리 파이**에서 **30분마다 자동으로 실행**되어 밀링 작업 로그 파일을 읽고, 외부 API 서버로 자동 전송하는 프로그램입니다.

---

## 🔄 동작 방식

### 1. 자동 실행 메커니즘 (Cron)

라즈베리 파이에는 **Cron**이라는 스케줄러가 내장되어 있습니다. 이는 Windows의 "작업 스케줄러"와 같은 역할을 합니다.

```
매 30분마다 자동 실행
00:00 → 실행
00:30 → 실행
01:00 → 실행
01:30 → 실행
... (무한 반복)
```

### 2. 실행 흐름

```
┌─────────────────────────────────────────┐
│  Cron (스케줄러)                        │
│  매 30분마다 자동 실행                  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  run_job.sh (실행 스크립트)             │
│  - 프로젝트 디렉토리로 이동              │
│  - 실행 시간 기록                        │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  server.js (메인 프로그램)               │
│  - 로그 파일 스캔                        │
│  - API 서버로 데이터 전송                │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│  scheduler.log (실행 로그)              │
│  - 실행 시간 기록                        │
│  - 실행 결과 기록                        │
└─────────────────────────────────────────┘
```

---

## 📁 주요 파일 및 역할

### 1. `run_job.sh` (실행 스크립트)
- **역할**: Cron에서 호출되는 실행 스크립트
- **위치**: `/home/datoz/Datoz-get-file-sys-node/run_job.sh`
- **동작**:
  1. 프로젝트 디렉토리로 이동
  2. 실행 시간을 `scheduler.log`에 기록
  3. `node server.js` 실행
  4. 완료 시간 기록

### 2. `server.js` (메인 프로그램)
- **역할**: 실제 로그 파일 처리 및 API 전송
- **동작**:
  1. 설정된 디렉토리에서 로그 파일 스캔
  2. API 서버에서 기존 주문 목록 조회
  3. 각 파일을 처리하여 API로 전송
  4. 결과를 로그 파일에 기록

### 3. `config/config.js` (설정 파일)
- **역할**: 시스템 설정 관리
- **주요 설정**:
  - `targetDirectory`: 로그 파일이 있는 폴더 경로
  - `apiUrl`: API 서버 주소
  - `filterDate`: 처리할 파일의 기준 날짜

### 4. `scheduler.log` (스케줄러 로그)
- **역할**: 자동 실행 기록
- **내용**: 실행 시간, 완료 시간, 실행 결과

### 5. `logs/log_YYYY-MM-DD.txt` (일별 로그)
- **역할**: 상세 처리 로그
- **내용**: 각 파일 처리 결과, API 전송 결과, 오류 정보

---

## 🔍 처리 과정 상세 설명

### Step 1: 파일 스캔
```
프로그램 시작
  ↓
설정된 디렉토리 스캔 (/home/datoz/Public/shared)
  ↓
JSON 파일 목록 수집
  ↓
날짜 필터 적용 (2025-09-20 이후 파일만)
```

### Step 2: 기존 주문 조회
```
API 서버에 GET 요청
  ↓
전체 주문 목록 받아오기 (예: 100건)
  ↓
메모리에 저장 (중복 체크용)
```

### Step 3: 각 파일 처리
```
각 JSON 파일마다:
  ↓
파일 읽기 및 파싱
  ↓
주문자 이름, 작업 시간 추출
  ↓
기존 주문 목록과 비교
  ↓
┌─────────────────────────────────────┐
│ 중복 없음? → CREATE API 호출       │
│ 이미 완료? → 건너뛰기               │
│ 작업중→완료? → UPDATE API 호출      │
└─────────────────────────────────────┘
```

### Step 4: 결과 기록
```
처리 결과를 로그 파일에 기록
  - 신규 생성: X건
  - 업데이트: Y건
  - 건너뛰기: Z건
  - 실패: W건
```

---

## 📊 로그 파일 확인 방법

### 1. 스케줄러 실행 로그 확인
```bash
# 실시간 로그 확인
tail -f ~/Datoz-get-file-sys-node/scheduler.log

# 최근 50줄 확인
tail -50 ~/Datoz-get-file-sys-node/scheduler.log
```

**로그 예시:**
```
----------------------------------------
실행 시간: 2025-10-15 07:30:01
[프로그램 실행 결과...]
완료 시간: 2025-10-15 07:30:15
========================================
```

### 2. 상세 처리 로그 확인
```bash
# 오늘 날짜의 로그 확인
cat ~/Datoz-get-file-sys-node/logs/log_$(date +%Y-%m-%d).txt

# 실시간 로그 확인
tail -f ~/Datoz-get-file-sys-node/logs/log_$(date +%Y-%m-%d).txt
```

### 3. 오류 로그 확인
```bash
# 오늘 날짜의 오류 로그 확인
cat ~/Datoz-get-file-sys-node/logs/errors/error_$(date +%Y-%m-%d).txt
```

---

## 🛠️ 관리 명령어

### Cron 작업 확인
```bash
# 등록된 cron 작업 목록 확인
crontab -l
```

**출력 예시:**
```
*/30 * * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh
```
→ 매 30분마다 실행됨을 의미

### 수동 실행 (테스트용)
```bash
# 1회만 실행 (테스트)
cd ~/Datoz-get-file-sys-node
./run_once.sh

# 또는 직접 실행
cd ~/Datoz-get-file-sys-node
node server.js
```

### Cron 작업 수정/삭제
```bash
# cron 작업 편집
crontab -e

# 특정 작업 삭제하려면 해당 줄을 삭제하고 저장
```

---

## ⚙️ 설정 변경 방법

### 1. 실행 주기 변경 (30분 → 다른 주기)

```bash
crontab -e
```

**변경 예시:**
```
# 30분마다 (현재)
*/30 * * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh

# 20분마다
*/20 * * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh

# 1시간마다
0 * * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh

# 매일 오전 9시, 오후 5시
0 9,17 * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh
```

### 2. 디렉토리 경로 변경

```bash
nano ~/Datoz-get-file-sys-node/config/config.js
```

**수정할 항목:**
```javascript
targetDirectory: '/home/datoz/Public/shared',  // 여기 수정
```

### 3. API 서버 주소 변경

```bash
nano ~/Datoz-get-file-sys-node/config/config.js
```

**수정할 항목:**
```javascript
apiListUrl: 'http://43.200.154.128/api/order/external/list',
apiUrl: 'http://43.200.154.128/api/order/external/create',
apiUpdateUrl: 'http://43.200.154.128/api/order/external/update',
```

### 4. 날짜 필터 변경

```bash
nano ~/Datoz-get-file-sys-node/config/config.js
```

**수정할 항목:**
```javascript
filterDate: '2025-09-20',  // 이 날짜 이후 파일만 처리
```

---

## 🔍 문제 해결

### 문제 1: Cron이 실행되지 않음

**확인 사항:**
```bash
# 1. Cron 서비스 상태 확인
sudo systemctl status cron

# 2. Cron 로그 확인
grep CRON /var/log/syslog

# 3. 등록된 작업 확인
crontab -l

# 4. 스크립트 실행 권한 확인
ls -la ~/Datoz-get-file-sys-node/run_job.sh
```

**해결 방법:**
```bash
# 실행 권한 부여
chmod +x ~/Datoz-get-file-sys-node/run_job.sh
```

### 문제 2: Node.js를 찾을 수 없음

**확인 사항:**
```bash
# Node.js 설치 확인
which node
node --version
```

**해결 방법:**
```bash
# Node.js 설치
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### 문제 3: API 연결 실패

**확인 사항:**
```bash
# API 서버 연결 테스트
curl http://43.200.154.128/api/order/external/list

# 네트워크 연결 확인
ping 43.200.154.128
```

**해결 방법:**
1. API 서버가 실행 중인지 확인
2. 방화벽 설정 확인
3. `config/config.js`의 API URL 확인

### 문제 4: 파일을 찾을 수 없음

**확인 사항:**
```bash
# 디렉토리 존재 확인
ls -la /home/datoz/Public/shared

# 설정 파일의 경로 확인
cat ~/Datoz-get-file-sys-node/config/config.js | grep targetDirectory
```

**해결 방법:**
1. `config/config.js`의 `targetDirectory` 경로 확인
2. 해당 폴더에 접근 권한 확인
3. 폴더 경로가 올바른지 확인

---

## 📈 모니터링 체크리스트

### 일일 확인 사항
- [ ] `scheduler.log`에서 정상 실행 확인
- [ ] 오류 로그(`logs/errors/`) 확인
- [ ] API 전송 성공 여부 확인

### 주간 확인 사항
- [ ] 로그 파일 크기 확인 (디스크 공간)
- [ ] 처리된 파일 개수 확인
- [ ] API 서버 응답 시간 확인

### 월간 확인 사항
- [ ] 오래된 로그 파일 정리
- [ ] 시스템 성능 확인
- [ ] 설정 파일 백업

---

## 📞 문의 및 지원

문제가 발생하거나 추가 설정이 필요한 경우:
1. 로그 파일 확인 (`scheduler.log`, `logs/log_*.txt`)
2. 오류 메시지 확인 (`logs/errors/error_*.txt`)
3. 개발팀에 문의 (로그 파일 첨부)

---

## ✅ 정상 동작 확인 방법

### 1. Cron 작업 확인
```bash
crontab -l
```
→ `*/30 * * * * /home/datoz/Datoz-get-file-sys-node/run_job.sh` 출력되어야 함

### 2. 최근 실행 확인
```bash
tail -20 ~/Datoz-get-file-sys-node/scheduler.log
```
→ 최근 실행 시간과 완료 시간이 기록되어 있어야 함

### 3. 처리 결과 확인
```bash
tail -50 ~/Datoz-get-file-sys-node/logs/log_$(date +%Y-%m-%d).txt
```
→ "처리 완료" 메시지와 통계가 표시되어야 함

---

## 📝 요약

1. **자동 실행**: Cron이 30분마다 자동으로 프로그램 실행
2. **파일 처리**: 설정된 디렉토리에서 로그 파일 읽기
3. **API 전송**: 외부 API 서버로 데이터 자동 전송
4. **로그 기록**: 모든 실행 내역이 로그 파일에 기록
5. **중복 방지**: 이미 처리된 파일은 자동으로 건너뛰기

**핵심**: 라즈베리 파이에 설치만 하면, 별도 조작 없이 30분마다 자동으로 실행되어 로그 파일을 처리하고 API로 전송합니다.




